name: Hello World Workflow

on:
  schedule:
    - cron: '*/5 * * * *'  # Runs every 5 minutes

jobs:
  say-hello:
    runs-on: ubuntu-latest

    steps:
      - name: Say Hello
        run: echo "Hello, World!"
