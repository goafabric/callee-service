server:
  port: 50900
  tomcat.max-threads: 20

management:
    endpoint:
      health:
        show-details: always
    endpoints:
      web:
        exposure:
          include: "info,health"

info:
    status: ok


security:
  authentication:
    enabled: true

#opentracing.jaeger.udp-sender.host: "localhost"
#opentracing.jaeger.udp-sender.port: "14250"
#opentracing.jaeger.service-name: "callee-service"
