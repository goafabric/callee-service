server:
  port: 50900
  tomcat.max-threads: 20

management:
    endpoint:
      health:
        show-details: always
    endpoints:
      web:
        exposure:
          include: "info,health"

info:
    status: ok


security:
  authentication:
    enabled: true

#jaeger
#opentracing.jaeger.disabled: "true"
#opentracing.jaeger.udp-sender.host: "jaeger-collector.istio-system.svc"
#opentracing.jaeger.udp-sender.port: "14268" #"14250"
#opentracing.jaeger.service-name: "callee-service"

#log levels
logging.level.root: "WARN"
logging.level.org.goafabric: "INFO"

logging.level.org.springframework: "INFO"
logging.level.org.flywaydb.core: "INFO"
logging.level.org.hibernate.SQL: "INFO"

#logging.level.io.opentracing: "DEBUG"

logging.pattern.console: "%white(%d{ISO8601}) %highlight(%-5level) [%blue(%t)] %yellow(%C{1.}): %msg%n%throwable"
